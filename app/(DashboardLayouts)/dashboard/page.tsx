import StatsCard from "@/lib/components/StatsCard";

export const dynamic = 'force-dynamic';

const page = () => {

    return (
        <div>
            <StatsCard />
        </div>
    );
};

export default page;